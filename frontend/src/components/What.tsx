export function What() {
  return (
    <div className="create-container pt-5 pb-0 px-5" id="what">
      <p>
        This tool allows you to interact with any deployed smart contract
        function on any EVM (Ethereum Virtual Machine) based blockchain, without
        an ABI. It utilizes your installed browser wallet (MetaMask).
      </p>
    </div>
  );
}
